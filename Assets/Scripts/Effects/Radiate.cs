using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 *		Radiate Effect
 *		Takes the shape of the object its attached to, which becomes bigger and fades out, creating a radiating effect.
 *		Created 20-01-2017 by Danny de Bruijne
 */

public class Radiate : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
